mod byte_sliceable_trait;
mod dev;
mod filter;
mod has_len_trait;
mod optimizer;
mod test_utils;

fn main() {
    // let k: usize = 5;
    //
    // let text1 = "hello hello how's life";
    // let text2 = "Sometimes life is a hard path";
    //
    // let all_strings = vec![text1, text2];
    // let all_sets = dev::convert_multiple_str_to_hashset(all_strings, k);
    // println!("{:?}", all_sets);
    //
    // let hashset1 = dev::convert_to_hashset(text1, k);
    // let hashset2 = dev::convert_to_hashset(text2, k);
    //
    // let has_intersection = hashset1.intersection(&hashset2).next().is_some();
    // println!("{:?}", has_intersection);
    // println!("{:?}", dev::convert_to_hashset(text1, k));
}
